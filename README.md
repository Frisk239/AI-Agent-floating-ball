# AI Agent Backend API v2.0

现代化AI助手后端API服务，基于FastAPI构建的智能自动化平台。

## 🚀 已实现功能特性

### ✅ **核心AI功能**
- **AI对话**: 支持与Moonshot Kimi、DashScope等大语言模型对话
- **智能文本分析**: 批量AI文本分析（总结、关键词提取、情感分析、分类、翻译）
- **语音交互**: 集成语音识别和语音合成
- **图像分析**: AI图像理解和OCR文字识别

### ✅ **自动化工具**
- **窗口管理**: 获取窗口列表、激活窗口、窗口切换、最小化/最大化/关闭
- **应用启动**: 智能应用启动（系统应用、浏览器、开发工具、社交软件等）
- **批量操作**: 批量启动应用、批量文本分析
- **快捷键操作**: 系统快捷键执行（复制、粘贴、搜索等）
- **剪切板管理**: 获取和设置剪切板内容，AI智能分析剪切板内容

### ✅ **文件处理**
- **文件夹管理**: 在指定路径批量创建文件夹
- **Office文档处理**: Word/Excel/PPT文件的AI分析和编辑
- **PDF处理**: PDF文档的AI分析和内容提取

### ✅ **系统集成**
- **上下文感知**: 智能系统状态监控和用户行为分析
- **性能监控**: CPU、内存、磁盘、网络使用情况实时监控
- **天气查询**: 实时天气信息获取
- **网页搜索**: 智能网页内容搜索和分析



## ✅ 测试验证状态

基于系统性API测试，已验证以下功能正常工作：

### 🧪 **已验证的核心功能** (8/10)

| 功能模块 | 测试状态 | 验证内容 |
|---------|---------|---------|
| **文件夹管理** | ✅ 已验证 | 批量创建文件夹，支持指定路径 |
| **剪切板管理** | ✅ 已验证 | 获取/设置内容，AI智能分析 |
| **应用启动** | ✅ 已验证 | 智能启动策略，批量启动应用 |
| **快捷键操作** | ✅ 已验证 | 复制粘贴等系统快捷键 |
| **窗口管理** | ✅ 已验证 | 窗口列表、激活、切换控制 |
| **上下文感知** | ✅ 已验证 | 系统状态监控，用户行为分析 |
| **批量文本分析** | ✅ 已验证 | AI并发分析，多种分析类型 |
| **批量应用启动** | ✅ 已验证 | 批量启动多个应用程序 |

### 🔄 **待验证功能**
- **语音交互**: 语音识别和语音合成
- **图像分析**: OCR文字识别和图像理解

### 📊 **测试统计**
- **总功能数**: 10个
- **已验证**: 8个 (80%)
- **测试通过率**: 100%
- **API响应**: 正常
- **并发处理**: 支持
- **错误处理**: 完善

## 🏗️ 技术架构

### 后端 (FastAPI + Python)
- **FastAPI**: 异步高性能Web框架
- **AI集成**: Moonshot Kimi、DashScope、秘塔搜索
- **RESTful API**: 完整的API接口设计
- **配置管理**: Pydantic配置验证
- **自动化工具**: 系统级操作和智能控制
- **并发处理**: 支持批量操作和多线程处理

## 📦 安装运行

### 环境要求
- **Python** >= 3.8.0

### 安装步骤
```bash
cd backend
pip install -r requirements.txt
python main.py
```

## 🎯 API使用方法

### 启动服务
```bash
cd backend
python main.py
```

### API基础信息
- **Base URL**: `http://localhost:8000`
- **认证方式**: 目前无需认证
- **数据格式**: JSON
- **响应格式**: JSON

### 快速测试
```bash
# 健康检查
curl "http://localhost:8000/api/health"

# 获取系统信息
curl "http://localhost:8000/api/system/info"
```

## 🛠️ 功能详细说明

### 🤖 AI智能分析
#### **批量文本分析**
- **功能**: 并发处理多个文本的AI智能分析
- **分析类型**: 总结、关键词提取、情感分析、文本分类、中文翻译
- **并发处理**: 支持5个并发请求，提高处理效率
- **使用示例**:
  ```bash
  curl -X POST "http://localhost:8000/api/automation/batch/analyze-texts" \
    -H "Content-Type: application/json" \
    -d '{"texts":["AI技术","机器学习"],"analysis_type":"总结"}'
  ```

#### **AI对话**
- **支持模型**: Moonshot Kimi、DashScope qwen系列
- **对话模式**: 流式响应，支持长文本对话
- **智能记忆**: 保持对话上下文

#### **图像分析**
- **OCR识别**: 屏幕截图文字提取
- **图像理解**: AI描述图像内容
- **格式支持**: PNG、JPG、JPEG

### ⚙️ 自动化工具
#### **窗口管理**
- **窗口列表**: 获取所有可见窗口信息
- **窗口控制**: 激活、最小化、最大化、关闭窗口
- **智能切换**: 基于历史记录的窗口切换
- **使用示例**:
  ```bash
  # 获取窗口列表
  curl "http://localhost:8000/api/automation/windows"

  # 激活窗口
  curl -X POST "http://localhost:8000/api/automation/windows/activate/1234"
  ```

#### **应用启动**
- **智能启动**: 支持预定义应用和搜索启动
- **批量启动**: 同时启动多个应用程序
- **支持应用**: 系统应用、浏览器、开发工具、社交软件等
- **使用示例**:
  ```bash
  # 启动单个应用
  curl -X POST "http://localhost:8000/api/automation/apps/launch" \
    -H "Content-Type: application/json" \
    -d '{"app_name":"notepad"}'

  # 批量启动应用
  curl -X POST "http://localhost:8000/api/automation/batch/launch-apps" \
    -H "Content-Type: application/json" \
    -d '{"app_names":["notepad","calc"]}'
  ```

#### **快捷键操作**
- **系统快捷键**: 复制、粘贴、撤销、全选等
- **批量执行**: 一次执行多个快捷键操作
- **跨平台支持**: Windows快捷键映射
- **使用示例**:
  ```bash
  curl -X POST "http://localhost:8000/api/automation/keyboard/shortcut" \
    -H "Content-Type: application/json" \
    -d '{"actions":["copy","paste"]}'
  ```

#### **剪切板管理**
- **内容获取**: 读取系统剪切板内容
- **内容设置**: 设置剪切板文本
- **AI分析**: 智能分析剪切板内容
- **使用示例**:
  ```bash
  # 获取剪切板内容
  curl "http://localhost:8000/api/automation/clipboard"

  # 设置剪切板内容
  curl -X POST "http://localhost:8000/api/automation/clipboard/set" \
    -H "Content-Type: application/json" \
    -d '{"content":"Hello World"}'
  ```

### 📁 文件处理
#### **文件夹管理**
- **批量创建**: 在指定路径创建多个文件夹
- **路径支持**: 指定基础路径或使用活动窗口路径
- **智能命名**: 自动处理文件名冲突
- **使用示例**:
  ```bash
  curl -X POST "http://localhost:8000/api/automation/folders/create" \
    -H "Content-Type: application/json" \
    -d '{"folder_names":["项目A","项目B"],"base_path":"C:\\Users\\Desktop"}'
  ```

#### **Office文档处理**
- **Word编辑**: AI辅助Word文档编辑
- **Excel处理**: 智能Excel文件分析
- **PPT分析**: 演示文稿内容提取
- **PDF处理**: PDF文档AI分析

### 📊 系统监控
#### **上下文感知**
- **系统状态**: CPU、内存、磁盘使用情况
- **用户活动**: 应用使用模式分析
- **智能建议**: 基于上下文的行动建议
- **使用示例**:
  ```bash
  # 获取上下文信息
  curl "http://localhost:8000/api/automation/context/info"

  # 获取智能建议
  curl "http://localhost:8000/api/automation/context/suggestions"
  ```

#### **性能监控**
- **实时监控**: CPU、内存、磁盘、网络状态
- **历史数据**: 系统性能趋势分析
- **阈值告警**: 性能异常检测

#### **天气查询**
- **实时天气**: 当前天气和预报
- **位置服务**: 基于地理位置的天气信息
- **多城市支持**: 支持多个城市的天气查询

## 🔧 核心功能实现

### AI批量文本分析 (Python实现)
```python
def batch_analyze_texts(texts: list, analysis_type: str = "总结") -> dict:
    """
    并发处理多个文本的AI智能分析
    支持总结、关键词提取、情感分析等多种分析类型
    """
    # 使用线程池并发处理
    with concurrent.futures.ThreadPoolExecutor(max_workers=5) as executor:
        # 提交分析任务
        futures = [executor.submit(analyze_single_text, text) for text in texts]
        # 收集结果
        results = [future.result() for future in concurrent.futures.as_completed(futures)]

    return {
        'results': results,
        'total_processed': len(texts),
        'success_count': len([r for r in results if r['success']]),
        'processing_time': time.time() - start_time
    }
```

### 智能应用启动 (Python实现)
```python
def launch_application_smart(app_name: str) -> dict:
    """
    智能应用启动器 - 双重启动策略
    1. 预定义应用映射启动
    2. Windows搜索启动
    """
    # 预定义应用映射
    app_commands = {
        "notepad": ["notepad.exe"],
        "calculator": ["calc.exe"],
        "chrome": ["start", "chrome"],
        # ... 更多应用
    }

    # 尝试预定义启动
    if app_name in app_commands:
        subprocess.Popen(app_commands[app_name], shell=True)
        return {"success": True, "method": "predefined"}

    # 搜索启动
    pyautogui.press('win')  # 打开开始菜单
    pyperclip.copy(app_name)  # 复制应用名
    pyautogui.hotkey('ctrl', 'v')  # 粘贴
    pyautogui.press('enter')  # 确认启动

    return {"success": True, "method": "search"}
```

## 📁 项目结构

```
AI-Agent-floating-ball/
├── backend/                    # FastAPI后端
│   ├── app/
│   │   ├── api/               # API路由
│   │   │   ├── automation.py  # 自动化功能API
│   │   │   ├── chat.py        # AI对话API
│   │   │   ├── speech.py      # 语音交互API
│   │   │   ├── vision.py      # 图像分析API
│   │   │   └── system.py      # 系统监控API
│   │   ├── core/              # 核心服务
│   │   │   ├── ai_clients.py  # AI模型客户端
│   │   │   └── config.py      # 配置管理
│   │   └── services/          # 业务服务
│   │       ├── automation/    # 自动化工具
│   │       ├── file_processing/ # 文件处理
│   │       ├── speech/        # 语音服务
│   │       ├── vision/        # 视觉服务
│   │       └── web/          # 网络服务
│   └── requirements.txt       # Python依赖
└── docs/                      # 项目文档
```

## 🔐 配置说明

### AI模型配置 (backend/config.json)
```json
{
  "ai": {
    "moonshot": {
      "api_key": "your-moonshot-api-key",
      "model": "kimi-k2-0905-preview"
    },
    "dashscope": {
      "api_key": "your-dashscope-api-key"
    }
  },
  "automation": {
    "gesture_enabled": true,
    "hotkey_enabled": true,
    "system_control_enabled": true
  }
}
```

## 🤝 贡献指南

1. Fork 项目
2. 创建功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 创建 Pull Request

## 📄 许可证

本项目采用 MIT 许可证 - 查看 [LICENSE](LICENSE) 文件了解详情。

## 🙏 致谢

- [FastAPI](https://fastapi.tiangolo.com/) - 现代Python Web框架
- [Pydantic](https://pydantic-docs.helpmanual.io/) - 数据验证和配置管理
- [Moonshot AI](https://moonshot.cn/) - AI大语言模型服务
- [DashScope](https://dashscope.aliyun.com/) - 阿里云AI服务
- [PyAutoGUI](https://pyautogui.readthedocs.io/) - Python自动化工具
- [OpenAI](https://openai.com/) - AI模型API兼容接口

---

**AI Agent Backend API** - 强大的AI助手后端服务！ 🤖⚡
